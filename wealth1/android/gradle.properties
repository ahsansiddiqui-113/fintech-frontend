# Gradle memory & performance settings
org.gradle.jvmargs=-Xmx4G -XX:MaxMetaspaceSize=2G -XX:+HeapDumpOnOutOfMemoryError
org.gradle.caching=true
org.gradle.daemon=true
org.gradle.parallel=true

# AndroidX migration (safe default)
android.useAndroidX=true
android.enableJetifier=true

# ✅ Use NDK version that supports 16 KB memory page sizes
android.ndkVersion=26.3.11579264

# Optional: Enable R8 shrinker for optimized release builds
android.enableR8=true
